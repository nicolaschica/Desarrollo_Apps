import { Routes } from '@angular/router';
import { ProductsPageComponent } from './features/products/products-page.component';

export const routes: Routes = [
  { path: 'car', component: ProductsPageComponent }
];
